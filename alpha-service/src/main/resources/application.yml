server:
  port: 8080

spring:
  application:
    name: alpha-service
  datasource:
    url: jdbc:h2:mem:alpha_db
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

management:
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  health:
    livenessState:
      enabled: true
    readinessState:
      enabled: true

logging:
  level:
    com.enterprise.alpha: INFO
    org.springframework: INFO
    org.springframework.boot.web.embedded.tomcat: INFO
